# 真实数据采集与评测记录

## 2026-08-08 招聘快照

采集范围限定为 5 家企业的官方招聘站点，关键词为“大模型”，不使用聚合招聘平台，也不绕过登录、验证码、限流或访问控制。

| 来源 | 记录数 | 职责完整 | 要求完整 |
|---|---:|---:|---:|
| 腾讯 | 32 | 32 | 32 |
| 美团 | 32 | 32 | 32 |
| 小米 | 32 | 32 | 32 |
| 百度 | 32 | 32 | 32 |
| 华为 | 12 | 12 | 12 |
| 合计 | 140 | 140 | 140 |

采集共发出 61 次 HTTP 请求，原始响应与完整 JD 仅保存在忽略目录 `data/real_eval/snapshots/2026-08-08/private/`。可交付的 `shareable/jobs.reference.jsonl` 只包含来源 ID、岗位 ID、标题、公司、地点、URL、时间和哈希。

结构验证通过以下门槛：不少于 100 条 JD、不少于 5 家企业、必填字段完整、内容哈希可重算、来源 ID 唯一、URL 属于 allow-list、未检测到电话或邮箱、公开索引无正文、双人模板覆盖完整、校准/测试拆分无精确重复泄漏。

## 外部证据语料

`sources/authoritative_sources.yaml` 固定列出 10 份政策/职业标准和 10 份权威报告。2026-08-08 的本地采集试跑结果：

- 20 个来源全部留下 URL、机构、时间、正文哈希和权利元数据。
- 18 个来源可自动解析为正文和证据，共 815 条证据块。
- `occupation-ai-engineer-notice-2019` 与 `policy-new-roles-2024` 的官方页面在普通 HTTP 客户端下只返回边缘包装页，当前标记为 metadata-only，不进入自动 RAG 评分。
- 参考索引为 `shareable/authoritative_documents.reference.jsonl`；它不含全文或证据正文。

PDF/DOCX/HTML 只在本地完成解析。未发现明确再分发许可的报告统一标为 `source-reference-only`，完整 PDF 和解析全文不进入 Git、Docker 镜像或交接包。

## 首次端到端结果

真实 JD 仓库的离线分析结果：

| 项目 | 数量 |
|---|---:|
| 统一文档 | 140 |
| 证据块 | 144 |
| 岗位观测 | 140 |
| 趋势特征 | 110 |
| 新岗位候选 | 0 |
| 岗位技能更新 | 0 |
| 图谱增量 | 0 |

`quality_report.json` 状态为 `valid`。零候选不是失败：新岗位门槛要求至少两个真实采集周，而本次只有一周。第二周后只能检验持续性门槛；完整 28 天近期窗口 + 84 天基线需要至少 16 个真实周快照。

## 当前可声称与不可声称

现在可以声称“真实官方来源数据已接通、140 条 JD 端到端结构校验通过、20 份权威外部来源完成证据化试跑”。

现在不能声称字段/技能 F1 ≥ 90%、近重 Precision ≥ 95%、RAG Recall@20 ≥ 90%、新岗位 P@10 ≥ 80% 或技能变化 F1 ≥ 85%。这些指标必须等待 A/B 双人标注、仲裁和后续真实周快照，不能用模型自评或空模板代替。
