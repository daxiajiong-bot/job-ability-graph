# 自动化测试报告

- 执行日期：2026-08-09
- 本地解释器：Python 3.13.13（项目最低要求 Python 3.11）
- 命令：`pytest -ra --cov=trend_discovery --cov-report=term-missing`
- 结果：69 passed
- 分支覆盖率：79.76%
- 验收门槛：60%，已通过

另行在隔离虚拟环境中安装 wheel，执行 `jobtrend --help` 和 120 条 JD 的纯离线 `run-all`，结果为：122 份文档、120 条岗位观测、21 条趋势特征、1 个 Agent 新岗位候选、1 个 Java 岗位能力更新。

标准组件包从 `tar.gz` 重新解压后再次执行 103 个 manifest artifact 哈希检查、wheel 安装、全量测试、CLI smoke 和离线演示，结果 `valid: true`；最终归档 SHA-256 以同名 `.tar.gz.sha256` 侧车文件为准。validator 另有 9 个纯离线篡改测试，覆盖 README、artifact 哈希、未登记文件、manifest 指纹、sidecar、不安全路径和符号链接，且确保预检失败时不会执行 pip。

组内 `INTERNAL-ONLY` 外层包的 152 个文件哈希已逐一复核；从解压目录导入得到 140 个真实官网文档 / 144 条证据，以及 10,510 个历史智联文档 / 11,166 条证据。再次分析真实官网快照得到 140 个岗位观测、110 条趋势特征、0 个新岗位和 0 个技能变化，`quality_report` 为 `valid`。

已验证真实 `jd_kg_v1` 图谱（84,269 节点 / 232,734 边）和 `small-raw-lskt-tech/v2` 图谱（3,736 节点 / 11,268 边）的只读导入、指纹、悬空边检查和原 ID 保留。

真实招聘评测快照另行完成 140 条 JD / 5 家企业的结构验证：140 条职责和要求完整，140 个来源内 ID 唯一，未检测到电话或邮箱；仓库往返为 140 个文档、144 条证据且无悬空引用。首次离线分析得到 140 个岗位观测和 110 条趋势特征，`quality_report` 为 `valid`。由于只有一个真实采集周，新岗位和技能变化均为 0，不将该结果冒充时序精度。

权威外部语料试跑完成 10 份政策/职业标准和 10 份行业报告：18 份正文可解析，共 815 条证据；2 份人社部网页只取得元数据，已排除自动 RAG 评分。公开参考索引不含正文。

警告：当前 scikit-learn 1.9 对 HDBSCAN `copy` 的未来默认值变化会输出 FutureWarning；一个流水线测试退出时还会报告 Qdrant 本地 SQLite 连接的 ResourceWarning，二者均不影响测试结果。真实业务 P/R/F1 不在本报告中冒充，需按标注指南完成双人金标后计算。

当前验证机未安装 Docker CLI，因此 Dockerfile 和非 root 入口已交付，但本报告不冒称已完成容器镜像构建。
