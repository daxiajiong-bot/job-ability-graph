# Demo 到 v3 的迁移说明

旧的规则解析、硬编码评分、JSON 图谱、样例页面、爬虫和数据库残留均不再属于运行路径。旧接口 `/samples`、`/parse/*`、`/match`、`/graph/*`、`/evolution/*`、`/jobs/*` 已移除，替换为 `/api/v1` 资源接口。

v3 不迁移旧数据文件，避免把简历原文和演示产物继续作为运行时状态。需要保留的历史工作区改动已在重构前保存为 Git stash。
